# 企業DB・架電リストの企業情報共有（2026-09-13）

## 目的
企業DBと各架電リストに重複している企業を、会社名だけでなく法人番号または会社名と代表者・電話・所在地の一致根拠で紐付け、同一企業の確認済み情報を参照できるようにする。共有された代表者自宅住所を、ブティックスを含む住所照合にも利用する。

## 手順
- [x] 企業DB・別リスト・取込元ファイルの重複と代表者住所の保有状況を確認
- [x] 元データ・出典を保持した企業ID、企業の紐付け、共有情報、矛盾を保留する仕組みを実装
- [x] 企業DBと架電先から共有情報を参照・更新できる画面と、横断した住所検索を実装
- [x] 既存データの紐付けと、今後の取込・更新時に反映される処理を実装
- [x] 誤名寄せ・権限・住所比較・大規模リストの性能を検証
- [x] 本番へ順次反映し、ブティックスと既存の正例を実画面で確認

## 方針
- 原本レコードや架電履歴を削除・上書きで統合しない。企業の対応関係と共有情報を別に保持する。
- 法人番号が矛盾する同名企業は自動統合しない。代表者住所は企業住所から推測しない。
- 組織・クライアントの既存の閲覧範囲を維持し、別クライアントの商談メモ等を公開しない。

## 確認結果・作業記録
- 企業DB491,292行、対象組織の架電リスト206,676行を読み取り専用で取得。
- 元Excelには代表者現住所があるが、企業DBの従来取込では落ちていた。元ファイルの377,093行を企業DBの識別情報・出典ファイルと照合して付加。紐付け不能18,350行、曖昧318行は付加しない。
- 検証用シードの企業数512,982社。企業DBのみ365,134、架電リストのみ23,401、両方124,447。代表者住所あり360,508、住所相違17、法人番号あり19,993。識別情報の矛盾233グループは別々に保持。
- ブティックス32,272行：一致8,006、不一致21,534、判定不可2,732。32,264行が企業DBに紐付く（残りは矛盾を保留）。本番画面の確認は未実施。
- 組織専用company_profiles/links/facts/events/reviewsを追加。グローバルcompany_masterに自宅住所を追加しない。
- 一括COPYの初回はWAL容量不足で失敗、全トランザクションをロールバック。新規テーブルが0行であることを確認した上で一時領域1,204MBを回収。元データは変更せず、読み書き正常への復帰を確認。
- Supabase管理画面で現在のディスク12GB・使用6.50GBを確認。max_wal_sizeを4GBから1GBへ再起動なしで変更（従来の追加設定は{}）。手動の容量増加・Compute変更は実施していない。
- シードは5,000行ずつコミット、DB+WALの監視上限9GiB（実ディスク12GB）で完了。全新規テーブルをVACUUM ANALYZE済み。途中再開は同じシードに限り--resume。DDLと大量取込を重ねない。
- 19:20に対象組織の自動同期を有効化。企業DB491,292行・架電206,676行について、基本情報とメモのハッシュを取得時点と比較。変更・新規・削除の取りこぼしはいずれも0。
- 本番SQL35項目成功。匿名・組織外アクセス拒否、同一カルテ参照、保存と競合拒否、新規取込・重複取込・架電先自動リンク、住所再判定、統合後の原本・出典・履歴保持、法人番号の矛盾による統合拒否。検証用書き込みは全てロールバック。
- 初期リンク索引はcompany_idがなく8,006行のヒープ読み込みが発生。INCLUDE(company_id)付き索引に修正し、同条件の先頭1,000件RPCは6,352msから1,774msへ改善。その後の測定は2,342〜2,907ms（同時負荷・キャッシュ状況による）。
- JSで生成した名寄せキーとPostgres関数の比較は1,789行で企業名・代表者・電話・住所・法人種別の差異0。
- ローカル393テスト成功（19:22）、最終ビルド成功。Supabaseの10マイグレーションを本番適用、履歴の時刻にローカルファイルを整合。
- 新規オブジェクトのアドバイザー確認：同期設定は管理者専用でポリシーなしを意図、認証ユーザー向けRPCは組織照合を実施。指摘されたlist_id外部キー索引を追加。未使用索引はリリース前のため保持。
- 法人番号は出典付き値・検査数字を使用。登記状態は未確認を初期値とし、確認済みにする場合は根拠を必須化。公的登記との全件自動照会と、従来の業種時間帯スコアの再集計は今回のCRM基盤には含まない。
- `6f2ef89`をmainへ反映、CI（34751920026）とVercel成功。Chromeの本番`index-aNfxSpGm-v3.js`で、共有企業一覧の4集計を確認。
- ブティックスの詳細モーダルに一致8,006・不一致21,534・判定不可2,732を表示。一致を選択して一覧モードに入り、8,006件・267ページと実際の企業行を確認。操作開始から11,882ms時点で全件・該当履歴の読み込みと描画が完了していた（完了瞬間の計測ではない）。
- 株式会社小林設備工業のカルテを架電リスト・企業DBの両方から開き、同一の代表者住所、元Excel行番号、4件の架電履歴、企業DBとブティックスの2出典を確認。実際の発信は実施していない。
- 既存の保険代理店①も詳細モーダルで一致2・不一致63・判定不可14を確認。一覧モードに従来の一致2件が表示されることを確認。
- 画面検証でハイフンなし電話検索の漏れを発見。電話番号専用索引・正規化検索を追加。全角数字のLIKEは遅くなるため、電話番号と判定できる入力は専用索引で検索。半角・ハイフン・全角・+81の4形式を認証ロールのRPCで検証し、同一企業IDの1件が返った。半角・全角は本番ブラウザーでも確認。
- 追加の4ケースはMCPで検証（検証端末のIPv6経路が切れたため、直接Postgresの全35ケースはこの時点で再実行せず）。既存35ケースを通した後の変更は検索関数と索引のみ。マイグレーション総数12。
- 集計には元データの企業名欠損・情報不足も残している。法的な存続確認を済ませた法人の確定件数ではなく、現データを保守的に名寄せした登録件数。「名寄せ後の登録企業」と表示し、未確定候補を分けて数えることを明記。

# 速度改善 第一段階

- [x] 不要なZoom認証を除き、録音取得・AI入力を維持
- [x] 録音再生の存在・型確認の往復を統合
- [x] 同じ利用者・録音の実行中署名要求をまとめる
- [x] 既存R2録音の再保存省略は保持期間と認可の同一性を確認して採否判断
- [x] fixture回帰テスト・同条件比較・ビルド・型チェック
- [ ] main経由の本番反映・読み取り確認・結果報告

モデル、プロンプト、音声、HP照合、認可/署名条件は維持。実録音の新規AI処理・負荷試験・無関係データ変更なし。

再保存省略は見送り。tasks/sekkei_recording_links.mdの実測で180日削除ルールが確認され、既存キー再利用では残存期間を維持できない。保存とAIの並行化も今回には含めず、第一段階を認証・再生前確認・同時要求集約に限定。

全314テスト（23ファイル）とビルド成功。旧版44e2e00と変更版を同じ8同時要求fixtureで実行し、署名要求8回→1回・返却値同一を確認。実通信・AI呼出しなし。秒数/改善率は未測定。

Deno 2.9.6型チェック: 新規recordingProbe.tsは成功。ハンドラー全体は既存の型エラー8件で失敗。変更前ソースでも同じ8件を再現済み（共有暗号バッファ型、既存管理操作のPromise参照等）。今回の新規エラーなし。認証・署名・AI処理のロジック修正には広げない。

公開前比較: transcribe-recording v49はindex本体がHEADと一致する一方、同梱共有ヘルパーがリポジトリより古く、署名鍵・保存先フォールバックが異なった。最初のCIを関数公開前に中止し、配布バージョン不変を確認。実際の本番ヘルパーを関数内recordingSource.tsへ同一内容で固定し、今回対象外の仕様変更を防止。架空の鍵で既存署名と両保存先分岐をテスト済み。

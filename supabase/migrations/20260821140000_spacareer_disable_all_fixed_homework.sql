-- ============================================================
-- スパキャリ 事後課題を「変動課題のみ」に（むー様 2026-08-21）
-- ------------------------------------------------------------
-- 直前の 20260821120000 で第1回の固定26問（homework_1テンプレ）の自動配信を廃止した。
-- 続けて第2回の固定10問も変動に寄せ、全ての回で固定課題ゼロ＝
-- 「議事録などをもとにAIで変動課題を生成 → トレーナーが修正 → 追加公開」に一本化する。
--
-- 外す10問（第2回）: 中〜高単価案件30件応募／クラウドワークスのプロフィール添削／
--   BizonとYenta導入／副業プラットフォーム5つ登録／信頼性の担保・商談スキル・痛みの
--   3つのアウトプット／特定企業の痛みリサーチ／痛みを補うツール作成とデプロイ／ツールの壁打ち。
--   これらのテーマはAI生成のプロンプト側にも例として入っているため、変動課題として出せる。
--
-- 行は消さず is_active=false にする（公開処理は is_active=true だけを見る）。
-- 戻したくなったら is_active=true に戻せば、その回から再び固定として自動配信される。
-- 既に受講生に配布済みの固定項目（spacareer_homework_items の source='fixed'）は別テーブルなので
-- 無変更＝過去の受講生の課題と回答はそのまま残る。
-- ============================================================
set local search_path = public, extensions;
set local lock_timeout = '5s';

update public.spacareer_homework_fixed_items
   set is_active = false,
       updated_at = now()
 where is_active = true;

# 作業のきまり

Codex・Claude Code など、このリポジトリで作業するエージェント向け。

## 着手前に読むもの

**`CLAUDE.md` を必ず読むこと。** このファイルはその入口で、中身はあちらにある。
過去の事故と対処は `tasks/lessons.md`。**同じ轍を踏まない。**

## 特に外せないもの

- **`git push origin main` は本番デプロイ。** 手で deploy しない。
- **本番は `main`。** ほかのブランチに出しても反映されない。
- **鍵は `.env.local`。** コードに書かない。画面にも会話にも出さない。
- **`DELETE` に `WHERE` を必ず書く。** 消す前に控えを取る。
- **大量に書き換えたら `VACUUM (ANALYZE)`。** しないと索引が効いていても画面が数百倍遅くなる。
- **重い流し込みと DDL を重ねない。** PostgREST が定義を読めず REST API が全面 503 になる。
- **`git add` は触ったパスだけ。** `git add -A` を使わない。
- 説明は日本語。画面の名前で話す。

## 進め方

- 3手以上かかる作業は、まず `tasks/` に計画を書く。
- **指摘を受けたら `tasks/lessons.md` に型として残す。**
- 数字は実測値だけ書く。**推定値を書かない。**
